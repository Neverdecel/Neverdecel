name: Build

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io

jobs:
  build-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate image tags
        id: tags
        run: |
          IMAGE_NAME=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          TIMESTAMP=$(date -u +%Y%m%d%H%M%S)
          SHA_SHORT=$(echo "${{ github.sha }}" | cut -c1-7)
          echo "image=${IMAGE_NAME}" >> $GITHUB_OUTPUT
          echo "timestamp=main-${TIMESTAMP}" >> $GITHUB_OUTPUT
          echo "sha=sha-${SHA_SHORT}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.tags.outputs.image }}:${{ steps.tags.outputs.timestamp }}
            ${{ env.REGISTRY }}/${{ steps.tags.outputs.image }}:${{ steps.tags.outputs.sha }}
            ${{ env.REGISTRY }}/${{ steps.tags.outputs.image }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
